export enum ControllerGroupIds {
    ENV,
    MODS,
    MAIN_DISP,
    OSC,
    FILTERS,
    SRC_MIX,
    FX,
    RING_MOD,
    NOISE,
    MASTER_CLOCK,
    ARP,
    KBD,
}

export enum ApiSource {
    UI,
    GUI,
    SPI,
    MIDI
}